{% extends "base.html.twig" %}
{% from "Components/Macros/autors.html.twig" import autorsComplet %}

{% block content %}
	<div class="grid grid-cols-1">
		<div id="obra-info">
			<h1>{{obra.titolOriginal}}</h1>
			{% if obra.titolCatala %}
				<h3>({{obra.titolCatala}})</h3>
			{% endif %}
			<h2>{{ autorsComplet(obra.autors) }}</h2>
		</div>
		<div id="llibres">
			<h1>Llibres</h1>
			{% include "Components/carrousselLlibres.html.twig" %}
		</div>
	</div>
{% endblock %}
